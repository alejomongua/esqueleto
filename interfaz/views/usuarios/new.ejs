<% formularios.errores(flash, error, {
    email: {
      label: 'Correo electrónico'
    },
    username: {
      label: 'Nombre de usuario'
    },
  }) %>
<% layout('../layout') -%>
<% if(typeof usuario === 'undefined') { var usuario = {} } %>
<%-partial('_form', {  
  usuario_actual: usuario_actual,
  usuario: usuario,
  error: error
  }) %>